logging:
  file:
    name: ./log/application.log
  level:
    org:
      springframework:
        jdbc:
          core: TRACE
        # web:
        #    filter:
        #        CommonsRequestLoggingFilter: DEBUG
        security: DEBUG

spring:
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 587
    username: 97389328915bf4
    password: c246a6b51fe964
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  data:
    redis:
      database: 1
      host: localhost
      port: 6379
  datasource:
    password: CHANGE_YOUR_PASSWORD
    tomcat:
      max-active: 50
      max-wait: 10000
      test-on-borrow: true
    url: jdbc:mysql://localhost/spring_session
    username: root
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    stomp:
      relay:
        host: localhost
        port: 61613
        login: guest
        passcode: guest
  security:
    public-key-path: classpath:keys/public.pub
    private-key-path: classpath:keys/private.key
    verification-token:
      expiration: 900000
    jwt:
      access-token:
        expiration: 900000
      refresh-token:
        expiration: 2592000000
    oauth2:
      client:
        registration:
          google:
            client-id: CHANGE_YOUR_GOOGLE_CLIENT_ID
            client-secret: CHANGE_YOUR_GOOGLE_SECRET_CLIENT_ID
            scope:
              - email
              - profile
            redirect-uri: ${GOOGLE_REDIRECT_URI:http://localhost:4300/auth/google/callback}
            authorization-grant-type: implicit
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://www.googleapis.com/oauth2/v4/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
            user-name-attribute: sub
      resourceserver:
        jwt:
          issuer-uri: https://accounts.google.com
          public-key-location:
springdoc:
  api-docs:
    path: /api-docs
---
spring.config.activate.on-profile: local
---
spring.config.activate.on-profile: docker-compose
spring:
  datasource:
    url: 'jdbc:mysql://mysql/app'
    username: app
    password: app
  data:
    redis:
      host: redis
      port: 6379
  rabbitmq:
    host: rabbitmq
    port: 5672
    stomp:
      relay:
        host: rabbitmq
        port: 61613
        login: guest
        passcode: guest